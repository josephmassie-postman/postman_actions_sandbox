$kind: collection
description: A small HTTP API that returns randomly generated JSON payloads.
variables:
  baseUrl: "{{baseUrl}}"
auth:
  - id: 02d5cb62-a2c3-4cc5-9662-40425af49657
    type: apikey
    name: apikey auth
    credentials:
      key: X-API-Key
      in: header
