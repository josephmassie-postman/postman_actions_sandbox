$kind: collection
description: A minimal mock API spec with a single endpoint for a friend list.
variables:
  baseUrl: "{{baseUrl}}"
auth:
  - id: 9552a146-985e-46c7-a3fb-9b971e2fef3a
    type: apikey
    name: apikey auth
    credentials:
      key: X-API-Key
      in: header
